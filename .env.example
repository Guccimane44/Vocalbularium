HOST=127.0.0.1
PORT=4318
DATA_DIR=.data
# Generation credentials belong only on the server.
# Use the key from your subscribed OpenCode Go workspace at https://opencode.ai/auth
OPENCODE_API_KEY=
OPENCODE_MODEL=deepseek-v4.1-flash
